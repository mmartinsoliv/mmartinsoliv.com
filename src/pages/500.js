import ErrorMessage from "../components/ErrorMessage";
import Base from "../layout/Base";

const customError = () => (
    <Base>
      <ErrorMessage message="five hundred..." />
    </Base>
)

export default customError